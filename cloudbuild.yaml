
# Auto-deploy configuration has been removed.
# This placeholder ensures that if a Cloud Build trigger runs, it consumes minimal resources and performs no action.
steps:
  - name: 'ubuntu'
    args: ['echo', 'Build skipped: Auto-deploy configuration has been removed from this repository.']
